let firstNumber = prompt('Give me first Number');
let secondNumber = prompt('Give me second Number');
let thirdNumber = prompt('Give me third Number');
let arithmeticMean = 0;
const number = 3;

Number(firstNumber);
Number(secondNumber);
Number(thirdNumber);

arithmeticMean = (firstNumber + secondNumber + thirdNumber) / number;

alert('Your number is: ' + arithmeticMean);